.module apple::iscntc: placed

.use defines::*
.use inline::*

.segment CODE
.if APPLE {
    .export .proc ISCNTC {
        lda $C000
        cmp #$83
        beq L0ECC
        rts
        L0ECC:
        jsr RDKEY
        cmp #$03
        .fallthrough flow1::STOP
    }
    ;!!! runs into "STOP"
}

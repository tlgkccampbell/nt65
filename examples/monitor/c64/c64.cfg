# A PRG that loads where BASIC programs do, with its variables after it. $FB to $FE are the
# zero page the KERNAL and BASIC leave free.
MEMORY {
    ZP:       file = "", start = $00FB, size = $0004;
    LOADADDR: file = %O, start = $07FF, size = $0002;
    MAIN:     file = %O, start = $0801, size = $97FF;
}
SEGMENTS {
    ZEROPAGE: load = ZP,       type = zp;
    LOADADDR: load = LOADADDR, type = ro;
    EXEHDR:   load = MAIN,     type = ro;
    STARTUP:  load = MAIN,     type = ro;
    CODE:     load = MAIN,     type = ro;
    RODATA:   load = MAIN,     type = ro;
    BSS:      load = MAIN,     type = bss;
}

.module header: placed

.segment RODATA
.export .data banner: .byte "BASIC", 0

; `tokens` is placed after this module, so ca65 has not seen `tokens::ENTRY` when it reaches
; this line, and the output writes its value instead.
.align tokens::ENTRY

MEMORY {
    ZP:   start = $0000, size = $0100;
    MAIN: start = $0800, size = $8000, file = %O;
}
SEGMENTS {
    ZEROPAGE: load = ZP, type = zp;
    CODE:     load = MAIN, type = ro;
    IMAGE:    load = MAIN, type = ro;
    COPIED:   load = MAIN, run = MAIN, define = yes;
}

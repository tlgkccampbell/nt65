; A module is one file.
.module hw::vic             ;! error[module-name-taken]: module `hw::vic` is already declared by `vic.nt65`: a module is one file, and a large one is split into submodules

.export .const FOREGROUND = $d022

.module second

.segment SHARED
.data other: .byte

; The second declaration is the one reported, whichever file it is in.
.segment DUP: abs         ;! error: segment "DUP" is already declared

; Exported, so that what the file only declares counts as used.
.export other

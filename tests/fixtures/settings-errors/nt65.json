{
  // The build sets a `.config` a module exports, by its qualified name, and nothing else.
  "files": ["*.nt65"],
  "defines": {
    "hw::HIDDEN": 1,      // ;! error[setting-not-exported]: `hw::HIDDEN` is not exported by module `hw`, so the build cannot set it
    "hw::MISSING": 1      // ;! error[setting-unknown]: `hw::MISSING` is not a `.config` setting of that module: the build can only set a setting a module declares and exports
  }
}

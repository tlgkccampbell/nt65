{
  // The build sets a setting a module declares, by its path, whether or not the module exports it.
  "files": ["*.nt65"],
  "settings": {
    "hw::HIDDEN": 1,
    "hw::MISSING": 1      // ;! error[setting-unknown]: `hw::MISSING` is not a setting of any module
  }
}

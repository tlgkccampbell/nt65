# The kinds the parser still builds the generic green node for, one to a line, sorted. Remove a
# kind's line in the same change that fixes its slots; ShapeTests reads the list strictly both
# ways, so a line that no longer stands for anything fails as loudly as a missing one.
ArgumentList
DataDirective
DataValues
ErrorLine
ExportDirective
ImportDirective
ListItems
MacroParameterList
ModuleDirective
NameExpression
NextDirective
ParameterKind
ParameterList
RecordValues
SegmentAttribute
SegmentBlock
SegmentDeclaration
SegmentRegion
SkippedTokens
StateItem
StateList
UseDirective
ValueList
